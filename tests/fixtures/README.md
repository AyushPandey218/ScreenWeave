# Synthetic fixtures

These images were generated for ScreenWeave testing and contain no user uploads.
`login.png` is the simple login fixture. `rounded-controls.png` is the browser-rendered rounded-input/button and curved-icon fixture. `rounded-layout.json` is its detected layout, used to test editing and export without running OCR again.
